---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: dify
  labels:
    app.kubernetes.io/name: dify
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app.kubernetes.io/name: dify
    app.kubernetes.io/component: web
