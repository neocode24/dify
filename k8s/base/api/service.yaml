---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: dify
  labels:
    app.kubernetes.io/name: dify
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 5001
    targetPort: 5001
    protocol: TCP
  selector:
    app.kubernetes.io/name: dify
    app.kubernetes.io/component: api
