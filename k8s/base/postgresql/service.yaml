---
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: dify
  labels:
    app.kubernetes.io/name: dify
    app.kubernetes.io/component: postgresql
spec:
  type: ClusterIP
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app.kubernetes.io/name: dify
    app.kubernetes.io/component: postgresql
